# Doctor console (doctor-app/) — browser-safe vars only
# Copy: cp doctor-app/.env.example doctor-app/.env

# Empty = use Vite proxy to platform API on :8000
VITE_API_BASE_URL=

# Optional: must match backend STAFF_TOKEN when set
VITE_STAFF_TOKEN=
VITE_STAFF_ROLE=doctor
